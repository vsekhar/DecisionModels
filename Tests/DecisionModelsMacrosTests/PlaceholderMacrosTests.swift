// Placeholder; replaced by the issue that owns the target

import Testing

@Suite("DecisionModelsMacros placeholder")
struct PlaceholderMacrosTests {
    @Test("the target builds")
    func targetBuilds() {
        #expect(Bool(true))
    }
}
