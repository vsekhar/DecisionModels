// Placeholder; replaced by the issue that owns the target

import Testing

@Suite("DecisionModelsTypeSafe placeholder")
struct PlaceholderTypeSafeTests {
    @Test("the target builds")
    func targetBuilds() {
        #expect(Bool(true))
    }
}
